import React from 'react';
import styled from 'styled-components';

const Box = styled.div`
  display: flex;
  justify-content: space-between;
  margin: 0 var(--margin-horizontal);
`;

export default function LabelMilestoneTab({ type }) {
  return <Box>123</Box>;
}
